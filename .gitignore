node_modules
dist
*.local
.DS_Store
coverage
docs/sessions/

# Personal floorplan photos dropped in the repo root (S25) — never publish these.
IMG_*.jpeg
IMG_*.jpg
IMG_*.png

# Finder/editor duplicates ("db 2.ts" and friends). One of these has been sitting
# in src/engine/ since 2026-07-28 and is imported by nothing. Twice in S29 a
# `git add src/` swept it into a LOCAL commit, and both times it was removed
# before anything was pushed — it has never been in a published commit, and this
# rule exists so a third time cannot happen. Ignored rather than deleted: it is
# the owner's file to remove.
*\ [0-9].ts
*\ [0-9].tsx
